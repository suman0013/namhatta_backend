# Development Profile Configuration

# Server Configuration
server.port=5000

# Database Configuration (Neon PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-calm-silence-a15zko7l-pooler.ap-southeast-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_5MIwCD4YhSdP&sslmode=require

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging - More verbose in dev
logging.level.root=INFO
logging.level.com.namhatta=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Actuator (if enabled)
management.endpoints.web.exposure.include=health,info

# Security - Less strict for development
spring.security.user.name=admin
spring.security.user.password=admin
