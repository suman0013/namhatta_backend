# Replit Environment Configuration
# Copy this to .env for Replit deployment

NODE_ENV=production
VITE_API_BASE_URL=https://your-repl-name.replit.app

# PostgreSQL Configuration for Replit
# Replace with your actual Replit PostgreSQL connection string
DATABASE_URL=postgresql://username:password@host:5432/database

# You can get this from Replit's database tab after creating a PostgreSQL database